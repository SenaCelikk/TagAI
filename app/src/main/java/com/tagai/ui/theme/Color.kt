package com.tagai.ui.theme

import androidx.compose.ui.graphics.Color

// --- The Paper Stack ---
val PaperBase = Color(0xFFF4F0E0)      // The main "Old Paper" color
val PaperDarker = Color(0xFFE8E2CA)    // For folded areas or card backgrounds
val PaperBorder = Color(0xFFDCD5B8)    // Subtle "edge" of the paper

// --- The Ink Stack (Never use pure black) ---
val InkBlack = Color(0xFF2C2924)       // Faded black ink
val InkFaded = Color(0xFF5D574D)       // Greyish ink for secondary text

// --- AI Accent (The "Magic" Stamp) ---
// Use a deep, royal purple that looks like a wax seal or premium ink
val VintagePurple = Color(0xFF634E96)
val VintagePurpleContainer = Color(0xFFE9E4F5)